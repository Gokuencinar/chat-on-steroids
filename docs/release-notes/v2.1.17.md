## Chat On Steroids 2.1.17

This release removes two false update reminders. ChatGPT connector refresh guidance is now tied to exact published tool-schema changes instead of the app version, while browser-extension compatibility follows the bridge protocol rather than release-number ordering. It also keeps the manual Releases link on the Gokuencinar fork and makes the connector reminder explicitly distinct from the browser extension.
